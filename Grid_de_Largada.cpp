#include <bits/stdc++.h>
using namespace std;
int t;
int n;
int arr[20002];
int brr[20002];
int main()
{
    cin>>t;
    while(t--)
    {
        cin>>n;
        for(int i=1;i<=n;i++) cin>>arr[i];
        for(int i=1;i<=n;i++) cin>>brr[i];
        sort(arr+1,arr+n+1);
        for(i)
    }
}